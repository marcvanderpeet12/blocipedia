<div class="well">
  <p>Title: <%= @wiki.title %></b></p>
  <p>Body: <%= @wiki.body %></b></p>
</div>
<% if policy(@wiki).edit? %>
<%= link_to "Edit", edit_wiki_path(@wiki), class: 'btn btn-success' %>
<% end %>